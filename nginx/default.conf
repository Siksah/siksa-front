# =========================================================
# 1. HTTP (80 포트) 블록: React 앱을 HTTP로 직접 서비스 (임시 설정)
# =========================================================
server {
    listen 80;
    server_name nyamnyam.kr www.nyamnyam.kr/;

    root /usr/share/nginx/html;
    index index.html index.htm; # index 파일 명시

    # ----------------------------------------------------
    # 👇 정적 파일 (Static Assets) 처리: 캐싱 및 리다이렉션 루프 방지
    # ----------------------------------------------------
    location ~* \.(?:ico|css|js|gif|jpe?g|png|woff2|woff|ttf|svg|eot)$ {
        expires 1y; 
        # 정적 파일이 없으면 404를 반환하고, SPA 폴백을 거치지 않도록 합니다.
        try_files $uri =404; 
    }

    # ----------------------------------------------------
    # 👇 프론트엔드 (React SPA) 서빙 및 폴백 로직 (핵심 수정)
    # ----------------------------------------------------
    location / {
        # 1. 요청된 URI($uri)를 찾는다. (예: /main.js)
        # 2. 요청된 URI 뒤에 /를 붙인 디렉토리를 찾는다. (예: /assets/)
        # 3. 위 두 가지 모두 실패하면, /index.html을 반환하여 SPA 폴백을 수행한다.
        #    (이 때 Nginx는 응답 코드를 200으로 유지합니다.)
        try_files $uri $uri/ /index.html;
    }
    
    # ----------------------------------------------------
    # 👇 API 요청 백엔드 (NestJS) 프록시
    # ----------------------------------------------------
    location /api {
        proxy_pass http://backend-app:3001;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto http;
    }
}
